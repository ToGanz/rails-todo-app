<% provide(:title, 'Edit Task') %>
<% provide(:button_text, 'Update Task') %>
<% provide(:path,  project_task_path(@project, @task) ) %>

<%= render 'tasks/form' %>

<%= link_to 'Delete Task', project_task_path(@project, @task), method: :delete,
 data: { confirm: "Are you sure?" }, class: "delete-task-link btn"%>